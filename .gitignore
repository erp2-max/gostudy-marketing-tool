# Dependencies
node_modules/

# Local dev only (vite dev server — not needed for static deployment)
package.json
package-lock.json
.claude/
開啟圖卡工具.bat

# Nested clone folder
gostudy-marketing-tool/

# Python scripts & generated outputs (only for local use)
test.py
ai生成/

# Company private assets (upload via the tool's upload feature instead)
公司內部資源/

# Legacy files
gostudy_standalone.html
